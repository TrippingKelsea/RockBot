[package]
name = "rockbot-credentials-schema"
version.workspace = true
edition = "2021"
description = "Credential schema types shared across RockBot provider plugins"
license = "MIT"

[lints]
workspace = true

[dependencies]
serde = { workspace = true }
